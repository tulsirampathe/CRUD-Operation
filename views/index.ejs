<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body> 
    <div class="bg-zinc-900 p-10 text-white">

        <h1 class="text-3xl tracking-tighter mb-3">Create User</h1>
        <form action="/create" method="post">
            <input class="px-5 py-2 mb-4 border-2 border-zinc-800 rounded-lg outline-none bg-transparent" type="text" placeholder="name.." name="name">
            <input class="px-5 py-2 mb-4 border-2 border-zinc-800 rounded-lg outline-none bg-transparent" type="text" placeholder="email.." name="email">
            <input class="px-5 py-2 mb-4 border-2 border-zinc-800 rounded-lg outline-none bg-transparent" type="text" placeholder="imag url.." name="image">

            <input class="px-5 py-2 bg-blue-500 rounded-lg ml-3" type="submit" name="" id="" value="Create user">
        </form>

        <h1 class="text-3xl mt-5 tracking-tighter mb-3">All Users</h1>
        <div class="users flex gap-3 flex-wrap">
            <% if(users.length > 0){ %>
                <% users.forEach(function(user){ %>
                    <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
                        <div class="w-full h-72 rounded-lg bg-zinc-600 overflow-hidden">
                            <img class="w-full h-full object-cover object-top" src="<%= user.image%>" alt=""></div>
                        <h3 class="tex-xl mt-3 tracking-tighter"><%= user.name %></h3>
                        <h5 class="text-zinc-500"><%= user.email %></h5>
                        <div class="flex gap-3 mt-10">
                            <a class="px-5 py-2 bg-yellow-500 rounded-lg text-zinc" href="/edit/<%= user._id%>">Edit User</a>
                            <a class="px-5 py-2 bg-red-500 rounded-lg text-black" href="/delete/<%= user._id %>">Delete User</a>
                        </div>
                    </div>
                <% }) %> 
            <% } else { %>
                <h2 class="text-zinc-500">No user yet</h2>
            <% } %>
        </div>

    </div>
</body>
</html>